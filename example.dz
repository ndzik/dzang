-- `Dzang` is a toy language for typed lambda calculus.

-- It allows for simple math:
x = 2 + 4;
y = 2 * 4;
print x; -- Prints 6.
print y; -- Prints 8.

-- Lambda functions.
add = \x y. x + y;
z = add 2 4;
print z; -- Prints 6.

-- Supports recursion.
fib = \n. if (n == 0) 0 if (n == 1) 1 (fib (n-2) + fib (n-1));
